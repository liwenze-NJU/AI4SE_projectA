# 线框图 00 — 全局应用外壳 (App Shell)

> 低保真线框图。所有页面共享此外壳：常驻 Mock 横幅 + 顶栏导航 + 内容插槽 + 底栏声明。
> 视觉令牌见 `DESIGN.md`；页面职责见 `INFORMATION_ARCHITECTURE.md`。

## 布局（桌面 1366+，1440 理想）

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│ 🛡 安全 Mock 演示模式   ·  未连接真实 Shell / 文件系统 / LLM / API Key / 凭据    │ ← 顶部 Mock 横幅（琥珀浅底，sticky，不可关闭）
├──────────────────────────────────────────────────────────────────────────────────┤
│ CodeGuard Harness   |  场景：B · 副作用动作待审批 ▾   |  ● AWAITING_APPROVAL 待审批   [ 返回场景 ] │ ← 顶栏导航
├──────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│                                                                                  │
│                          ▼  页面主内容插槽（P1 / P2 / P4 在此渲染）  ▼           │
│                                                                                  │
│                                                                                  │
│                                                                                  │
├──────────────────────────────────────────────────────────────────────────────────┤
│ ScriptedMockLLM · 离线确定性回放 · 不接触真实环境                                │ ← 底栏声明（mono）
└──────────────────────────────────────────────────────────────────────────────────┘
```

## 区域图例

| 区域 | 作用 | 状态 |
|---|---|---|
| 顶部 Mock 横幅 | 持续显示安全演示模式与未连接项 | **常驻**，无关闭/折叠，四屏一致 |
| 顶栏 · 品牌字标 | `CodeGuard Harness`（非 Vercel logo） | 静态 |
| 顶栏 · 场景下拉 | 切换/重选场景 | `[默认][悬停][焦点]`；当前场景名 mono |
| 顶栏 · 当前态药丸 | 实时反映运行态 | 随 P2/P4 状态变化（色+图标+文字） |
| 顶栏 · 返回场景 | 回 P1 | 次按钮 |
| 内容插槽 | 各页主体 | — |
| 底栏声明 | 离线确定性回放声明 | 静态 mono |

## 变体

### 窄屏 / 手机（<768px）顶栏折叠
```
┌──────────────────────────────────────────┐
│ 🛡 安全 Mock 演示模式 · 未连接真实环境   │ ← 横幅永不折叠
├──────────────────────────────────────────┤
│ CodeGuard Harness        ☰  [ 返回场景 ] │ ← 场景下拉收进 ☰ 抽屉
├──────────────────────────────────────────┤
│   （主内容单列堆叠）                      │
├──────────────────────────────────────────┤
│ ScriptedMockLLM · 离线确定性回放         │
└──────────────────────────────────────────┘
```

## 状态说明
- Mock 横幅在 `prefers-reduced-motion`、Windows 高对比度模式下均保持可见可读（文字+图标，不依赖动画或仅色）。
- 顶栏当前态药丸在 P3 模态打开期间仍显示 `AWAITING_APPROVAL 待审批`，与模态内信息一致。
