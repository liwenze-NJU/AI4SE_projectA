# REFLECTION

## 一、任务完成情况

这次我的任务是完成一个 Coding Agent Harness。一开始我以为 Harness 只是一个能调用大模型和执行代码的 Agent，后来才逐渐理解，它更重要的作用是管理 Agent：模型负责提出动作，Harness 负责判断动作是否安全、能否执行，以及执行后是否真正完成目标。

我最终完成了 CodeGuard Harness，包括状态机、安全护栏、人工审批、工具调用、测试反馈、停止条件、记忆、配置、API Key 管理和运行记录，并将其打包为 Windows 可执行文件 `codeguard.exe`。项目还提供本地 WebUI，用于展示 Agent 的运行过程。

项目包含三个 Mock 演示场景：场景 A 展示越界操作被 BLOCK 后改为安全操作；场景 B 展示文件修改需要人工审批；场景 C 展示测试失败、反馈修复和再次测试通过的过程。最终全量测试结果为 626 passed、1 skipped，跳过项是 Windows 无提升权限时无法创建符号链接，不属于主要功能失败。

## 二、关键设计和实现想法

项目最重要的设计是显式状态机。Agent 会话被分成构建上下文、决策、安全检查、等待审批、执行、测试反馈和最终验证等状态。这样每一步都可以被观察和记录，Agent 也不能只凭自己声称完成就结束，必须通过最终验证才能进入 COMPLETED。

安全护栏会在动作执行前进行检查。访问工作区外的路径会被直接阻止，修改文件等有副作用的操作可能需要人工审批。审批结果还会与当前会话和动作指纹绑定，避免被错误地用于其他动作。

此外，我把演示环境与真实环境分开。测试和 WebUI 使用确定性的 Mock 数据，不读取真实文件，也不调用真实 Shell 或 API。只有用户主动选择 local 模式并配置 API Key 时，程序才会尝试使用真实模型。

## 三、开发过程中遇到的问题和 AI 的帮助

这个项目的大部分开发过程都有 AI 辅助。我使用 Claude Code、Codex 和 DeepSeek 帮助我分析要求、编写 SPEC 和 PLAN、生成测试、实现代码以及做代码评审。AI 在查找问题和生成基础代码方面确实很快，也发现了一些我自己很难想到的安全问题。

但是，AI 也给我带来了一个很明显的问题，就是项目被设计得越来越复杂。最开始我反复让 AI 审查 SPEC、PLAN 和设计文件，每个很小的 Task 也要单独实现、测试和评审。虽然看起来非常规范，但是花费的时间和 Token 都很多。我做了三天后甚至还没有真正开始主要代码，后来学校提供的模型额度也用完了。

之后我开始调整做法。我换用了价格更低的模型，把几个相关的 Task 放在一批完成，并且只要求 Critical 和 Major 问题必须返工，不再因为变量命名或小优化反复修改。这让我意识到，计划和评审并不是越多越好。如果前期流程太复杂，反而可能影响真正的开发和交付。

另外，我也发现 AI 说“完成了”不一定代表真的可以使用。例如 WebUI 最开始点击“步进”后会不断回到初始化状态。原因是前端自己修改了状态，但后端一直返回 INITIALIZING，轮询时又把前端状态覆盖了。后来把后端改成唯一的状态来源才解决。

还有一次，场景 A 的 BLOCK 和 ALLOW 同时被加入结果中，页面只显示最后一个 ALLOW，导致看不到前面的 BLOCK。场景 B 也出现过进入等待审批后找不到审批入口的问题。这些问题都是我自己打开网页、一步一步操作后发现的。AI 生成的自动化测试虽然都通过了，但仍然不能完全代替人工测试。

打包 EXE 时也遇到了类似问题。源码运行正常，但打包后 WebUI 无法启动，原因包括 Uvicorn 的导入方式和资源路径变化。后来通过真实运行 `codeguard.exe web` 并访问网页，才发现并修复这些问题。

## 四、对要求的完成情况

完成项目后，我按照 SPEC 和课程要求进行了检查。状态机、安全护栏、人工审批、工具执行、反馈闭环、记忆和停止条件都有相应的实现和测试。三个演示场景也分别展示了 BLOCK、REQUEST_APPROVAL 和测试失败后修复的过程。

CLI 提供了 chat、demo、web、key 和 config 等命令。API Key 通过隐藏输入保存到系统凭据管理中，不会直接写到仓库、配置文件或日志中。WebUI 顶部也明确写着 Mock 演示模式，避免使用者误以为它正在操作真实文件。

最终项目中包括源码、测试、README、SECURITY、SPEC、PLAN、AGENT_LOG、REFLECTION、EXE 和 SHA-256 校验文件。我还运行了全量测试、凭据扫描和 EXE 冒烟测试，并人工检查了三个网页场景。GitHub Actions、GitLab CI 和打包配置已经准备，但公网 Render 部署没有实际完成。目前计划通过 GitHub Release 提供 EXE 下载。

## 五、不足和后续改进

目前项目还有一些比较明显的不足。虽然命令中有 `chat`，但它现在只会运行一次 Harness 会话，并输出最后的状态，不是我一开始想象的那种可以一直输入问题、持续显示 Agent 回复的聊天工具。项目也没有模型选择菜单、流式输出和完整的聊天记录。

WebUI 现在主要是展示 Harness 工作机制，使用的是提前设计好的 Mock 场景，不能直接让用户输入一个真实编程任务。Windows EXE 没有数字签名，可能出现 SmartScreen 提示。CI 配置虽然写好了，但还需要在推送到远程仓库后继续检查实际运行结果。

因为课程提供的模型和 API Token 额度有限，后期我把主要精力放在完成核心要求、安全机制和测试上，没有继续开发多轮聊天等功能。这保证了课程要求可以完成，但项目的实际使用体验还不够完整。

如果以后继续开发（比如等我的codex额度刷新了），我希望在新分支中增加真正的多轮 CLI，让用户可以选择模型、输入任务并看到流式回复。同时也可以让 WebUI 连接真实 Harness 会话，而不只是回放 Mock 场景。

通过这次项目，我最大的感受是，使用 AI 写代码并不代表人可以完全不检查。AI 很适合生成代码和提供思路，但也可能把项目设计得过于复杂，或者在功能还不能真正使用时就报告完成。最后仍然需要人来决定需求范围、控制成本，并通过测试和实际操作确认结果，尤其是控制成本，怎么尽量有更少的token保质保量的完成任务，毕竟以后也不能花钱上班。